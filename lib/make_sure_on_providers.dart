import 'package:flutter/material.dart';

class CheckProvider extends StatelessWidget {
  const CheckProvider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
